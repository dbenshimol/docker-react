language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t davidbigdata/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true davidbigdata/docker-react npm run test
